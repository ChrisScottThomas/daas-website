# Airtable API access
AIRTABLE_API_KEY=your_airtable_api_key
AIRTABLE_BASE_ID=your_airtable_base_id
AIRTABLE_TABLE_NAME=Invoices

# AWS SES email sender (must be verified in SES)
SES_FROM_EMAIL=clarity@getclarity.win

# Stripe secret key (optional for now, required for card payments)
STRIPE_SECRET_KEY=your_stripe_secret_key

# Your site’s base URL (used for redirects, email links, etc.)
PUBLIC_SITE_URL=https://getclarity.win
