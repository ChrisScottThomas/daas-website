---
import Base from "../layouts/base.astro";
---

<Base title="Clarity. — Insight-as-a-Service">
  <main class="max-w-4xl mx-auto px-4 py-24 text-center">
    <h1 class="text-4xl font-bold mb-4 leading-tight">
      Clarity. Without the Noise.
    </h1>
    <p class="text-lg text-gray-600 dark:text-gray-300 mb-8">
      Join the waitlist for Clarity. — your Strategic Insight-as-a-Service
      Partner. Clear support plans designed for product and enterprise leaders
      who need faster answers, tighter focus, and confident decisions.
    </p>

    <form id="waitlist-form" class="space-y-4 max-w-md mx-auto">
      <input
        type="email"
        name="email"
        required
        placeholder="you@example.com"
        class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring focus:ring-blue-200"
      />
      <button
        type="submit"
        class="w-full py-2 px-4 bg-black text-white rounded font-medium hover:bg-gray-800 transition"
      >
        Join the Waitlist
      </button>
    </form>
    <p id="response" class="mt-4 text-green-600"></p>

    <script>
      document
        .getElementById("waitlist-form")
        .addEventListener("submit", async (e) => {
          e.preventDefault();
          const email = e.target.email.value;
          const res = await fetch("API_URL_HERE", {
            method: "POST",
            body: JSON.stringify({ email }),
            headers: { "Content-Type": "application/json" },
          });
          if (res.ok) {
            window.location.href = "/thank-you";
          } else {
            document.getElementById("response").textContent =
              "Something went wrong. Please try again.";
          }
        });
    </script>

    <!-- Add the rest of your sections below here: Trusted by, Why, Testimonials, CTA -->

    <!-- Trusted by section -->
    <section class="mt-16">
      <p
        class="text-sm uppercase tracking-wide text-gray-500 dark:text-gray-400 mb-4"
      >
        Trusted by product leaders at
      </p>
      <div class="flex flex-wrap justify-center gap-6 opacity-80">
        <img src="/placeholder-logo1.svg" alt="Logo 1" class="h-8" />
        <img src="/placeholder-logo2.svg" alt="Logo 2" class="h-8" />
        <img src="/placeholder-logo3.svg" alt="Logo 3" class="h-8" />
        <img src="/placeholder-logo4.svg" alt="Logo 4" class="h-8" />
      </div>
    </section>

    <!-- Value grid -->
    <section class="mt-20 text-left">
      <h2 class="text-2xl font-bold mb-6 text-center">
        Why Insight-as-a-Service?
      </h2>
      <div class="grid md:grid-cols-3 gap-8">
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Outcome Clarity.</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            Know exactly what to build and why. No more guessing, scope creep,
            or building the wrong thing.
          </p>
        </div>
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Embedded Expertise</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            Our team integrates into yours. No bloated decks or outside
            observers. Just delivery-ready, actionable insight.
          </p>
        </div>
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Predictable Cost</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            Frictionless support plans. Fixed scope, high impact, no unexpected
            invoices.
          </p>
        </div>
      </div>
    </section>

    <!-- Testimonials -->
    <section class="mt-20 text-center">
      <h2 class="text-2xl font-bold mb-6">What Our Clients Say</h2>
      <div class="max-w-3xl mx-auto">
        <blockquote
          class="italic text-lg text-gray-700 dark:text-gray-300 mb-6"
        >
          “Before working with them, we were guessing what to build. Their
          Discovery process gave us clarity, confidence, and speed. We cut 3
          months off our roadmap.”
        </blockquote>
        <p class="text-sm text-gray-500 dark:text-gray-400">
          - Head of Product, Fintech Scaleup
        </p>
      </div>
    </section>

    <!-- Final CTA -->
    <section
      class="mt-20 bg-gray-900 text-white py-12 px-6 rounded-lg text-center"
    >
      <h2 class="text-2xl font-bold mb-4">Still thinking about it?</h2>
      <p class="mb-6 text-lg">
        Sign up for more information. <br>The sooner you're on the list, the sooner
        you'll stop wasting time on the wrong problems. <br><br>Join now. No fluff, no
        spam, just Clarity.
      </p>
      <a
        href="#waitlist-form"
        class="inline-block bg-white text-black font-semibold px-6 py-3 rounded hover:bg-gray-200 transition"
        >Join the Waitlist</a
      >
    </section>
  </main>
</Base>
