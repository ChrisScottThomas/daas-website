---
import Base from "../layouts/base.astro";
---

<Base title="Clarity. — Insight-as-a-Service">
  <main class="max-w-4xl mx-auto px-4 py-24 text-center">
    <h1 class="text-4xl font-bold mb-4 leading-tight">
      Clarity. Without the Noise.
    </h1>
    <p id="sub-header" class="text-lg text-gray-600 dark:text-gray-300 mb-8">
      Join the waitlist for Clarity. — your Strategic Insight-as-a-Service
      Partner. Clear support plans designed for product and enterprise leaders
      who need faster answers, tighter focus, and confident decisions.
    </p>

    <form id="waitlist-form" class="space-y-4 max-w-md mx-auto">
      <input
        type="email"
        name="email"
        required
        placeholder="your@futurepartner.com"
        class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring focus:ring-blue-200"
      />
      <button
        type="submit"
        class="w-full py-2 px-4 bg-black text-white rounded font-medium hover:bg-gray-800 transition"
      >
        Join the Waitlist
      </button>
    </form>
    <p id="response" class="mt-4 text-green-600"></p>

    <script>
      document
        .getElementById("waitlist-form")
        .addEventListener("submit", async (e) => {
          e.preventDefault();
          const email = e.target.email.value;
          const res = await fetch("API_URL_HERE", {
            method: "POST",
            body: JSON.stringify({ email }),
            headers: { "Content-Type": "application/json" },
          });
          if (res.ok) {
            window.location.href = "/thank-you";
          } else {
            document.getElementById("response").textContent =
              "Something went wrong. Please try again.";
          }
        });
    </script>

    <!-- waitlist Feature Flag -->

    <script>
      const waitlistEnabled = false;

      document.addEventListener("DOMContentLoaded", () => {
        const form = document.getElementById("waitlist-form");
        const button = document.querySelector("#waitlist-form button");

        if (!waitlistEnabled) {
          form.addEventListener("submit", (e) => {
            e.preventDefault();
            alert("Waitlist is currently closed. Check back soon!");
          });
          button.disabled = true;
          button.classList.add("opacity-50", "cursor-not-allowed");
          button.textContent = "Waitlist Closed";
        }
      });
    </script>

    <!-- Trusted by section -->
    <section id="trusted-by" class="mt-16">
      <p
        class="text-sm uppercase tracking-wide text-gray-500 dark:text-gray-400 mb-4"
      >
        Trusted by product leaders at
      </p>
      <div class="flex flex-wrap justify-center gap-6 opacity-80">
        <img src="/placeholder-logo1.svg" alt="Logo 1" class="h-8" />
        <img src="/placeholder-logo2.svg" alt="Logo 2" class="h-8" />
        <img src="/placeholder-logo3.svg" alt="Logo 3" class="h-8" />
        <img src="/placeholder-logo4.svg" alt="Logo 4" class="h-8" />
      </div>
    </section>

    <script>
      const showTrustedBy = false;

      document.addEventListener("DOMContentLoaded", () => {
        if (!showTrustedBy) {
          const section = document.getElementById("trusted-by");
          if (section) section.remove(); // or section.style.display = "none";
        }
      });
    </script>

    <!-- Value grid -->
    <section class="mt-20 text-left">
      <h2 class="text-2xl font-bold mb-6 text-center">
        Why Insight-as-a-Service?
      </h2>
      <div class="grid md:grid-cols-2 gap-8">
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Outcome Clarity.</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            Know exactly what to build and why. <br />No more guessing, scope
            creep, or wasted effort.
          </p>
        </div>
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Embedded Expertise.</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            We integrate into your team, not just your calendar.<br /> No bloated
            decks. Just actionable insight.
          </p>
        </div>
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Predictable Cost.</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            Frictionless support plans with fixed scope. <br />High impact. No
            surprises. No extras.
          </p>
        </div>
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Strategic Continuity.</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            No stop-start engagements or endless SoWs. <br />Consistent
            thinking, context & momentum.
          </p>
        </div>
      </div>
    </section>

    <!-- Value grid -->
    <section class="mt-20 text-left">
      <h2 class="text-2xl font-bold mb-6 text-center">Why Clarity.?</h2>
      <div class="grid md:grid-cols-2 gap-8">
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Real Understanding.</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            Your language. Your constraints. Your goals. <br />
            Faster traction. Less translation.
          </p>
        </div>
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Confident Decisions.</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            We combine proven frameworks with lived experience. <br />Clear.
            Justified. Actionable recommendations.
          </p>
        </div>
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Faster Progress.</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            You'll leave each engagement with tangible outputs:<br /> Priorities.
            Blockers. Next steps. Immediate progress.
          </p>
        </div>
        <div class="p-6 border rounded-lg bg-white dark:bg-black/20 shadow-sm">
          <h3 class="font-semibold text-lg mb-2">Zero Overhead.</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300">
            Our approach is designed to extract insight quickly. <br />You get
            value without adding more to your plate.
          </p>
        </div>
      </div>
    </section>

    <!-- Testimonials -->
    <section id="testimonials" class="mt-20 text-center">
      <h2 class="text-2xl font-bold mb-6">What Our Clients Say</h2>
      <div class="max-w-3xl mx-auto">
        <blockquote
          class="italic text-lg text-gray-700 dark:text-gray-300 mb-6"
        >
          “Before working with them, we were guessing what to build. Their
          Discovery process gave us clarity, confidence, and speed. We cut 3
          months off our roadmap.”
        </blockquote>
        <p class="text-sm text-gray-500 dark:text-gray-400">
          - Head of Product, Fintech Scaleup
        </p>
      </div>
    </section>

    <script>
      const showTestimonials = false;

      document.addEventListener("DOMContentLoaded", () => {
        if (!showTestimonials) {
          const section = document.getElementById("testimonials");
          if (section) section.remove(); // or section.style.display = "none";
        }
      });
    </script>

    <!-- Final CTA -->
    <section
      class="mt-20 bg-gray-900 text-white py-12 px-6 rounded-lg text-center"
    >
      <h2 class="text-2xl font-bold mb-4">Still unsure?</h2>
      <p class="mb-6 text-lg">
        That’s why Clarity exists.<br /> You don’t need all the answers, just a better
        way to find them. <br /><br />No fluff. No spam. Just Clarity.<br /><br
        />
      </p>
      <a
        href="#sub-header"
        class="inline-block bg-white text-black font-semibold px-6 py-3 rounded hover:bg-gray-200 transition"
        >Join the Waitlist</a
      >
    </section>
  </main>
</Base>
